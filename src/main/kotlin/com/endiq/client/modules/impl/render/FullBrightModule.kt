package com.endiq.client.modules.impl.render

import com.endiq.client.modules.Module

/** Applied only while generating the lightmap, never written into options.txt. */
class FullBrightModule : Module("Full Bright","Boosts the client lightmap while enabled",Category.RENDER)
